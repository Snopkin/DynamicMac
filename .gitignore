# macOS
.DS_Store

# Xcode user state
xcuserdata/
*.xcuserstate
*.xcuserdatad/

# Build artifacts
build/
DerivedData/
*.xcarchive

# Swift Package Manager
.swiftpm/
.build/
# Package.resolved is intentionally committed — it's the SPM dependency lock file.

# Playground
timeline.xctimeline
playground.xcworkspace

# Instruments
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# CocoaPods / Carthage (not used, but harmless)
Pods/
Carthage/Build/

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png


project.pbxproj

# Build secrets (API key — never commit the real plist, only the template)
DynamicMac/Secrets.plist
